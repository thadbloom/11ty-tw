<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <meta name="description" content="">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="{{ STATIC_PATH }}/assets/style.css">
</head>
<body>
  <div class="max-w-2xl mx-auto prose">
    <header>
      <h2 class="text-4xl font-bold">{{ title }}</h2>
    </header>
    <main>
      {{ content | safe }}
    </main>
    <footer>
      <p><small>&copy; {% year %}</small></p>
    </footer>
  </div>
</body>
</html>
